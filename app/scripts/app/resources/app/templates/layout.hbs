<div class="{{class}}">
    {{#each rows}}
        <div class="{{class}}">
            {{#each columns}}
                <div id="{{name}}" class="{{class}}"></div>
            {{/each}}
        </div>
    {{/each}}
</div>